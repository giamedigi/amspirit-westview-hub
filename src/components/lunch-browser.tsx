"use client";import{useState}from"react";import type{LunchMonth}from"@/lib/types";
export function LunchBrowser({months}:{months:LunchMonth[]}){const[index,setIndex]=useState(1);const current=months[index];const label=new Date(current.year,current.month-1).toLocaleDateString("en-US",{month:"long",year:"numeric"});return <><div className="month-controls"><button onClick={()=>setIndex(i=>Math.max(0,i-1))} disabled={index===0} aria-label="Previous month">←</button><h2>{label}</h2><button onClick={()=>setIndex(i=>Math.min(months.length-1,i+1))} disabled={index===months.length-1} aria-label="Next month">→</button></div><div className="lunch-grid">{current.groups.map((g,i)=><article className={`lunch-card tone-${i%4}`} key={g.id}><span>Group {i+1}</span><ul>{g.members.map(n=><li key={n}>☕ {n}</li>)}</ul></article>)}</div></>}
