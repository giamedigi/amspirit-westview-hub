"use client";import{useMemo,useState}from"react";import type{MemberEvent}from"@/lib/types";import{appConfig}from"@/config/app";
const key=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;
export function CalendarView({events}:{events:MemberEvent[]}){const[start,setStart]=useState(new Date(2026,7,1));const[selected,setSelected]=useState("2026-08-05");const cells=useMemo(()=>{const first=new Date(start.getFullYear(),start.getMonth(),1);const count=new Date(start.getFullYear(),start.getMonth()+1,0).getDate();return [...Array(first.getDay()).fill(null),...Array.from({length:count},(_,i)=>new Date(start.getFullYear(),start.getMonth(),i+1))]},[start]);const selectedDate=new Date(`${selected}T12:00:00`);const selectedEvents=events.filter(e=>e.date===selected);const isMeeting=selectedDate.getDay()===4;const change=(amount:number)=>setStart(new Date(start.getFullYear(),start.getMonth()+amount,1));return <><div className="calendar-toolbar"><button onClick={()=>change(-1)} aria-label="Previous month">←</button><h2>{start.toLocaleDateString("en-US",{month:"long",year:"numeric"})}</h2><button onClick={()=>change(1)} aria-label="Next month">→</button><button className="today-button" onClick={()=>{setStart(new Date(2026,6,1));setSelected("2026-07-23")}}>Today</button></div><div className="calendar-grid" role="grid" aria-label={`${start.toLocaleDateString("en-US",{month:"long",year:"numeric"})} calendar`}>{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(d=><div className="weekday" role="columnheader" key={d}>{d}</div>)}{cells.map((date,i)=>date?<button role="gridcell" aria-selected={key(date)===selected} className={key(date)===selected?"selected":""} onClick={()=>setSelected(key(date))} key={key(date)}><span>{date.getDate()}</span><span className="dots" aria-label={`${(date.getDay()===4?1:0)+events.filter(e=>e.date===key(date)).length} events`}>{date.getDay()===4&&<i className="meeting-dot"/>}{events.some(e=>e.date===key(date))&&<i className="event-dot"/>}</span></button>:<span className="empty" key={`e${i}`}/>)}</div><section className="selected-events" aria-live="polite"><h2>{selectedDate.toLocaleDateString("en-US",{weekday:"long",month:"long",day:"numeric"})}</h2>{isMeeting&&<article><span className="badge meeting">Weekly meeting</span><h3>AM Spirit West View Chapter Meeting</h3><p>{appConfig.meeting.startTime} to {appConfig.meeting.endTime}<br/><strong>{appConfig.meeting.venue}</strong><br/>{appConfig.meeting.addressLine1}<br/>{appConfig.meeting.addressLine2}</p><a className="text-link" href={appConfig.directions.url} target="_blank" rel="noreferrer">{appConfig.directions.label}<span className="sr-only"> (opens in a new tab)</span></a></article>}{selectedEvents.map(e=><details className="event-detail" key={e.id} open><summary>{e.title}</summary><p><strong>{e.startTime} to {e.endTime}</strong>{e.location&&<><br/>{e.location}</>}</p><p>{e.description}</p>{e.registrationLink&&<a className="button primary" href={e.registrationLink} target="_blank" rel="noreferrer">Registration link</a>}</details>)}{!isMeeting&&!selectedEvents.length&&<p className="empty-state">No events on this date.</p>}</section></>}
