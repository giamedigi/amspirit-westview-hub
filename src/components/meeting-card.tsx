import Link from "next/link"; import {appConfig} from "@/config/app";
export function MeetingCard({compact=false}:{compact?:boolean}){const m=appConfig.meeting;return <section className={`meeting-card ${compact?"compact":""}`} aria-labelledby="meeting-title"><div><span className="eyebrow">Weekly chapter meeting</span><h2 id="meeting-title">{m.day}s</h2><p className="meeting-time">{m.startTime} to {m.endTime}</p><address><strong>{m.venue}</strong><br/>{m.addressLine1}<br/>{m.addressLine2}</address></div><div className="button-row"><Link className="button light" href="/meeting-information">Meeting Information</Link><a className="button outline-light" href={appConfig.directions.url} target="_blank" rel="noreferrer">{appConfig.directions.label}<span className="sr-only"> (opens in a new tab)</span></a><Link className="button outline-light" href="/invite-a-guest">Invite a Guest</Link></div></section>}
