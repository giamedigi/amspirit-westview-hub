import Link from "next/link";import type{Announcement}from "@/lib/types";
export function AnnouncementCard({item,compact=false}:{item:Announcement;compact?:boolean}){return <article className={`announcement-card ${item.importance}`}><div className="card-top"><span className="badge">{item.importance==="urgent"?"⚠ Urgent":item.importance==="important"?"● Important":"Normal"}</span><time dateTime={item.publishDate}>{new Date(`${item.publishDate}T12:00:00`).toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"})}</time></div><h2>{item.title}</h2><p>{compact?`${item.message.slice(0,100)}${item.message.length>100?"…":""}`:item.message}</p><Link className="text-link" href={`/announcements/${item.id}`}>Read announcement <span aria-hidden="true">→</span></Link></article>}
