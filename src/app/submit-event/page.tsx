import{PageHeading}from"@/components/page-heading";import{appConfig}from"@/config/app";export const metadata={title:"Submit an Event"};export default function Page(){return <><PageHeading title="Submit an Event">Share a member business or community event for possible inclusion on the chapter calendar.</PageHeading><section className="callout-card"><span className="callout-icon" aria-hidden="true">＋</span><h2>Have an event to share?</h2><p>Use the submission form to send the event title, date, time, location, and details. The live form will be connected in a later phase.</p><a className="button primary large" href={appConfig.submitEvent.url} aria-disabled={appConfig.submitEvent.placeholder}>{appConfig.submitEvent.label}</a><small>Submission link placeholder — no information is collected yet.</small></section></>}
