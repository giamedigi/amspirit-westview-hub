import Link from"next/link";import{PageHeading}from"@/components/page-heading";import{MeetingCard}from"@/components/meeting-card";import{appConfig}from"@/config/app";export const metadata={title:"Meeting Information"};export default function Page(){const m=appConfig.meeting;return <><PageHeading title="Meeting Information">Everything you need for a Thursday morning visit.</PageHeading><MeetingCard/><section className="detail-card"><h2>Visiting the chapter</h2><p>Guests are welcome. Arrive a few minutes early so a chapter member can greet you and help you get settled. Bring business cards if you have them.</p><a className="map-placeholder" href={appConfig.directions.url} target="_blank" rel="noreferrer" aria-label={`${appConfig.directions.label} to ${m.venue} (opens in a new tab)`}><span aria-hidden="true">⌖</span><strong>{appConfig.directions.label}</strong><small>{m.venue}<br/>{m.addressLine1}<br/>{m.addressLine2}</small></a><div className="button-row"><Link className="button primary" href="/invite-a-guest">Invite a Guest</Link><a className="button secondary" href={appConfig.portal.url} target="_blank" rel="noreferrer">{appConfig.portal.label}</a></div></section></>}
